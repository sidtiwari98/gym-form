import ContactSheet from "@/components/ContactSheet";

/**
 * Visual QA sheet: every exercise, both views, start and end of the rep.
 * Not linked from anywhere in the app — it exists to eyeball all the poses at
 * once when tuning the rig.
 */
export default function DevPage() {
  return <ContactSheet />;
}
